[project]
name = "privstrip-pyref"
version = "0.1.0"
description = "Reference inference for openai/privacy-filter, used to validate the Rust port."
requires-python = ">=3.11,<3.12"
dependencies = [
    "opf",
    "torch",
    "tiktoken",
]

[tool.uv.sources]
opf = { git = "https://github.com/openai/privacy-filter.git" }
